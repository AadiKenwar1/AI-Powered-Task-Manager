# Copy to ".env" in this folder (gitignored). Never commit real keys.

# Use ONE of the following (OPEN_AI_KEY is preferred by this project):
OPEN_AI_KEY=
# OPENAI_API_KEY=

# Optional override:
# OPENAI_MODEL=gpt-4o-mini
